                                    //DATA TYPES
public class vid7 {
    public static void main(String a[])
    {
        System.out.println("hello"); 
        /*
         * we will talk about primitive data types for now:
         * primitive:
         * ->INTEGER
         * ->CHARACTER
         * ->FLOAT
         * ->BOOLEAN
         * 
         * Integer->
         * a>int -> 4 bytes
         * b>long -> 8 bytes
         * c>short -> 2 bytes
         * d>byte -> 1 byte(-2^7 to 2^7-1 or -128 to 127)
         * 
         * float -> 4 bytes
         * double -> 8 bytes
         * 
         * we use float to store decimal values with low precision.
         * we use double to store decimal values with high precision.
         * 
         * note we cannot directly store decimal values to float.
         * we have to add 'f' at the end.
         * syntax: float var = valuef;
         * 
         * syntax for double:
         * double var = value;
         * 
         * char-> 2 bytes
         * syntax:
         * char var = 'k';
         * 
         * boolean -> true or false.
         * note like other languages we do not consider 0 and 1 for true and false.
         * syntax:
         * boolean x=true;
         * boolean x=false;
         * 
         * note in case of long we need to add l at the end of value:
         * 
         */
    }
}
